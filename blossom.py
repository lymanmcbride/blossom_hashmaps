class hashmap:
    def __init__(size):
        self.array_size = size
        self.array = []

    def hash(self, key):
        sum(key.encode())
    def compress(hash_code):
        result = hash_code % self.array_size
        return result
    
